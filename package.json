{
  "name": "student-booking-services",
  "private": true,
  "version": "0.1.0",
  "description": "WSU Student Booking Services — CPTS 489 Final",
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm --workspace client run dev",
    "dev:server": "npm --workspace server run dev",
    "build": "npm --workspace shared run build && npm --workspace client run build",
    "test": "npm --workspace server test && npm --workspace client test",
    "test:server": "npm --workspace server test",
    "test:client": "npm --workspace client test"
  }
}
